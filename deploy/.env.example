# Public URL of your site (same origin serves the storefront and the Frappe API).
FRAPPE_URL=https://shop.yourdomain.com

# Name of the existing Frappe docker network to join (run `docker network ls`).
# frappe_docker usually creates "<project>_default", e.g. frappe_docker_default.
FRAPPE_NETWORK=frappe_docker_default
